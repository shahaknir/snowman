#include <string>
#include <iostream>
#include <stdexcept>

const int MAXIMUM_LENGTH = 8, MINIMUM_LENGTH = 1, MAX_CHAR = 4, MIN_CHAR = 1, BASE = 10;

namespace ariel {
    bool corInput(int x);
    std::string snowman(int num);
}